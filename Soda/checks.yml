checks:
  - name: Dataset contains data
    condition: row_count > 0
    target:
      table: My_Table

  - name: No NULL values in Num column
    condition: missing_count(Num) = 0
    target:
      table: My_Table
      column: Num
